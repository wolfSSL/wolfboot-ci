name: Build and Publish CI Images

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:
    inputs:
      image:
        description: 'Image to build (all, base, arm, sim, m33mu, powerpc, riscv, aarch64, x86, renode)'
        required: false
        default: 'all'

env:
  REGISTRY: ghcr.io
  OWNER: wolfssl

jobs:
  base:
    if: >-
      github.event_name == 'push' ||
      github.event.inputs.image == 'all' ||
      github.event.inputs.image == 'base'
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    steps:
      - uses: actions/checkout@v7

      - name: Log in to GHCR
        uses: docker/login-action@v4
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GHCR_PAT }}

      - name: Build and push base
        uses: docker/build-push-action@v7
        with:
          context: base
          push: true
          tags: |
            ${{ env.REGISTRY }}/${{ env.OWNER }}/wolfboot-ci-base:${{ github.ref_name }}
            ${{ env.REGISTRY }}/${{ env.OWNER }}/wolfboot-ci-base:latest

  arm:
    needs: base
    if: >-
      github.event_name == 'push' ||
      github.event.inputs.image == 'all' ||
      github.event.inputs.image == 'arm'
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    steps:
      - uses: actions/checkout@v7

      - name: Log in to GHCR
        uses: docker/login-action@v4
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GHCR_PAT }}

      - name: Build and push arm
        uses: docker/build-push-action@v7
        with:
          context: arm
          push: true
          build-args: BASE_TAG=${{ github.ref_name }}
          tags: |
            ${{ env.REGISTRY }}/${{ env.OWNER }}/wolfboot-ci-arm:${{ github.ref_name }}
            ${{ env.REGISTRY }}/${{ env.OWNER }}/wolfboot-ci-arm:latest

  sim:
    needs: base
    if: >-
      github.event_name == 'push' ||
      github.event.inputs.image == 'all' ||
      github.event.inputs.image == 'sim'
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    steps:
      - uses: actions/checkout@v7

      - name: Log in to GHCR
        uses: docker/login-action@v4
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GHCR_PAT }}

      - name: Build and push sim
        uses: docker/build-push-action@v7
        with:
          context: sim
          push: true
          build-args: BASE_TAG=${{ github.ref_name }}
          tags: |
            ${{ env.REGISTRY }}/${{ env.OWNER }}/wolfboot-ci-sim:${{ github.ref_name }}
            ${{ env.REGISTRY }}/${{ env.OWNER }}/wolfboot-ci-sim:latest

  m33mu:
    if: >-
      github.event_name == 'push' ||
      github.event.inputs.image == 'all' ||
      github.event.inputs.image == 'm33mu'
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    steps:
      - uses: actions/checkout@v7

      - name: Log in to GHCR
        uses: docker/login-action@v4
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GHCR_PAT }}

      - name: Build and push m33mu
        uses: docker/build-push-action@v7
        with:
          context: m33mu
          push: true
          pull: true
          no-cache: true
          tags: |
            ${{ env.REGISTRY }}/${{ env.OWNER }}/wolfboot-ci-m33mu:${{ github.ref_name }}
            ${{ env.REGISTRY }}/${{ env.OWNER }}/wolfboot-ci-m33mu:latest

  powerpc:
    needs: base
    if: >-
      github.event_name == 'push' ||
      github.event.inputs.image == 'all' ||
      github.event.inputs.image == 'powerpc'
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    steps:
      - uses: actions/checkout@v7

      - name: Log in to GHCR
        uses: docker/login-action@v4
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GHCR_PAT }}

      - name: Build and push powerpc
        uses: docker/build-push-action@v7
        with:
          context: powerpc
          push: true
          build-args: BASE_TAG=${{ github.ref_name }}
          tags: |
            ${{ env.REGISTRY }}/${{ env.OWNER }}/wolfboot-ci-powerpc:${{ github.ref_name }}
            ${{ env.REGISTRY }}/${{ env.OWNER }}/wolfboot-ci-powerpc:latest

  riscv:
    needs: base
    if: >-
      github.event_name == 'push' ||
      github.event.inputs.image == 'all' ||
      github.event.inputs.image == 'riscv'
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    steps:
      - uses: actions/checkout@v7

      - name: Log in to GHCR
        uses: docker/login-action@v4
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GHCR_PAT }}

      - name: Build and push riscv
        uses: docker/build-push-action@v7
        with:
          context: riscv
          push: true
          build-args: BASE_TAG=${{ github.ref_name }}
          tags: |
            ${{ env.REGISTRY }}/${{ env.OWNER }}/wolfboot-ci-riscv:${{ github.ref_name }}
            ${{ env.REGISTRY }}/${{ env.OWNER }}/wolfboot-ci-riscv:latest

  aarch64:
    needs: base
    if: >-
      github.event_name == 'push' ||
      github.event.inputs.image == 'all' ||
      github.event.inputs.image == 'aarch64'
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    steps:
      - uses: actions/checkout@v7

      - name: Log in to GHCR
        uses: docker/login-action@v4
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GHCR_PAT }}

      - name: Build and push aarch64
        uses: docker/build-push-action@v7
        with:
          context: aarch64
          push: true
          build-args: BASE_TAG=${{ github.ref_name }}
          tags: |
            ${{ env.REGISTRY }}/${{ env.OWNER }}/wolfboot-ci-aarch64:${{ github.ref_name }}
            ${{ env.REGISTRY }}/${{ env.OWNER }}/wolfboot-ci-aarch64:latest

  x86:
    needs: base
    if: >-
      github.event_name == 'push' ||
      github.event.inputs.image == 'all' ||
      github.event.inputs.image == 'x86'
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    steps:
      - uses: actions/checkout@v7

      - name: Log in to GHCR
        uses: docker/login-action@v4
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GHCR_PAT }}

      - name: Build and push x86
        uses: docker/build-push-action@v7
        with:
          context: x86
          push: true
          build-args: BASE_TAG=${{ github.ref_name }}
          tags: |
            ${{ env.REGISTRY }}/${{ env.OWNER }}/wolfboot-ci-x86:${{ github.ref_name }}
            ${{ env.REGISTRY }}/${{ env.OWNER }}/wolfboot-ci-x86:latest

  renode:
    needs: base
    if: >-
      github.event_name == 'push' ||
      github.event.inputs.image == 'all' ||
      github.event.inputs.image == 'renode'
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    steps:
      - uses: actions/checkout@v7

      - name: Log in to GHCR
        uses: docker/login-action@v4
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GHCR_PAT }}

      - name: Build and push renode
        uses: docker/build-push-action@v7
        with:
          context: renode
          push: true
          build-args: BASE_TAG=${{ github.ref_name }}
          tags: |
            ${{ env.REGISTRY }}/${{ env.OWNER }}/wolfboot-ci-renode:${{ github.ref_name }}
            ${{ env.REGISTRY }}/${{ env.OWNER }}/wolfboot-ci-renode:latest
